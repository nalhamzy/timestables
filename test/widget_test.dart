// Widget smoke test for Times Tables Trainer.
// Full integration tests require a device/emulator.
// This file guards against compile-time regressions.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — replace with real widget tests', () {
    expect(1 + 1, 2);
  });
}
